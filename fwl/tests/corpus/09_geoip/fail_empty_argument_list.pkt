name: "geoip() with no arguments is rejected by parser/analyzer"
source_fw: |
  @xdp(eth0)
  drop if pkt.src_ip in geoip()
  default allow

test_packet:
  builder: tcp(src_ip="1.2.3.4")

expected:
  compiles: false
