name: "Tier 2: bare port read inside proto guard is OK"
source_fw: |
  @xdp(eth0)
  def firewall(pkt):
    if pkt.proto == tcp:
      port = pkt.dst_port
      if port == 22:
        drop
    allow

test_packet:
  builder: tcp(dst_port=22)

expected:
  compiles: true
  bpf_action: drop
