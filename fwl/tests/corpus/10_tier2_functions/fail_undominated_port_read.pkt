name: "Tier 2 compile error: bare port read without proto guard"
source_fw: |
  @xdp(eth0)
  def firewall(pkt):
    port = pkt.dst_port
    drop

test_packet:
  builder: tcp(dst_port=22)

expected:
  compiles: false
