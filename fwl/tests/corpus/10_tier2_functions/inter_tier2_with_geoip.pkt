name: "Tier 2 + geoip: drop traffic from RU"
source_fw: |
  @xdp(eth0)
  def firewall(pkt):
    if pkt.src_ip in geoip(RU):
      drop
    allow

geoip_data:
  RU: ["5.8.0.0/16"]

test_packet:
  builder: tcp(src_ip="5.8.1.1", dst_port=80)

expected:
  compiles: true
  bpf_action: drop
