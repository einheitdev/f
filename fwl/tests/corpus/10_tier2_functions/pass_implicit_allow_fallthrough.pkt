name: "Tier 2: function with no terminal falls through to implicit allow"
source_fw: |
  @xdp(eth0)
  def firewall(pkt):
    if pkt.proto == tcp:
      log

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: allow
