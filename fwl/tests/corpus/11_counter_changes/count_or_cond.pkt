name: "counter OR condition"
source_fw: |
  @xdp(eth0)
  count web if pkt.proto == tcp and pkt.dst_port in [80, 443]
  allow

test_packet:
  builder: tcp(dst_port=443)

expected:
  compiles: true
  bpf_action: allow
  counter_changes:
    web: 1
