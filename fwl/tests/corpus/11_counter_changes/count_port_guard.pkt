name: "counter with port guard"
source_fw: |
  @xdp(eth0)
  count http if pkt.proto == tcp and pkt.dst_port == 80
  allow

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: allow
  counter_changes:
    http: 1
