name: "TCP flag rule does not match a UDP packet (proto guard short-circuits)"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.tcp.rst
  default allow

test_packet:
  builder: udp(dst_port=53)

expected:
  compiles: true
  bpf_action: allow
