name: "pkt.tcp.ack set on a guarded TCP packet drops"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.tcp.ack
  default allow

test_packet:
  builder: tcp(ack=true)

expected:
  compiles: true
  bpf_action: drop
