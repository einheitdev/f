name: "pkt.tcp.cwr set on a guarded TCP packet drops"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.tcp.cwr
  default allow

test_packet:
  builder: tcp(cwr=true)

expected:
  compiles: true
  bpf_action: drop
