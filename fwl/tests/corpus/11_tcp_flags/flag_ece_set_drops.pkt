name: "pkt.tcp.ece set on a guarded TCP packet drops"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.tcp.ece
  default allow

test_packet:
  builder: tcp(ece=true)

expected:
  compiles: true
  bpf_action: drop
