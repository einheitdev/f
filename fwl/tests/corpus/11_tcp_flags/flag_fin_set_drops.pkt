name: "pkt.tcp.fin set on a guarded TCP packet drops"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.tcp.fin
  default allow

test_packet:
  builder: tcp(fin=true)

expected:
  compiles: true
  bpf_action: drop
