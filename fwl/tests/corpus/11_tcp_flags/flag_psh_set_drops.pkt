name: "pkt.tcp.psh set on a guarded TCP packet drops"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.tcp.psh
  default allow

test_packet:
  builder: tcp(psh=true)

expected:
  compiles: true
  bpf_action: drop
