name: "pkt.tcp.rst set on a guarded TCP packet drops"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.tcp.rst
  default allow

test_packet:
  builder: tcp(rst=true)

expected:
  compiles: true
  bpf_action: drop
