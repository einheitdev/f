name: "pkt.icmp.type rule does not match a UDP packet"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == icmp and pkt.icmp.type == 8
  default allow

test_packet:
  builder: udp(dst_port=53)

expected:
  compiles: true
  bpf_action: allow
