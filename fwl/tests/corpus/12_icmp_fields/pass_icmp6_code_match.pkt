name: "pkt.icmp6.code == 0 matches an ICMPv6 echo request's zero code"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == icmp6 and pkt.icmp6.code == 0
  default allow

test_packet:
  builder: icmp6(type=128, code=0)

expected:
  compiles: true
  bpf_action: drop
