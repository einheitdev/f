name: "pkt.icmp.type != 8 drops non-echo ICMP"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == icmp and pkt.icmp.type != 8
  default allow

test_packet:
  builder: icmp(type=3)

expected:
  compiles: true
  bpf_action: drop
