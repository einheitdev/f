name: "ct bare ack invalid then established 10.44.0.3 1024 203.0.113.77 443"
source_fw: |
  @xdp(eth0)
  drop if conntrack(pkt).state == invalid
  allow if conntrack(pkt).state == established
  allow if pkt.proto == tcp and pkt.tcp.syn and pkt.dst_port == 443
  default drop
sequence:
  - name: bare ack, no entry (invalid -> drop)
    builder: tcp(src_ip="10.44.0.3", dst_ip="203.0.113.77", src_port=1024, dst_port=443, ack=true)
    expected: { bpf_action: drop }
  - name: syn (new, allowed, creates entry)
    builder: tcp(src_ip="10.44.0.3", dst_ip="203.0.113.77", src_port=1024, dst_port=443, syn=true)
    expected: { bpf_action: allow }
  - name: same bare ack again (now established)
    builder: tcp(src_ip="10.44.0.3", dst_ip="203.0.113.77", src_port=1024, dst_port=443, ack=true)
    expected: { bpf_action: allow }
