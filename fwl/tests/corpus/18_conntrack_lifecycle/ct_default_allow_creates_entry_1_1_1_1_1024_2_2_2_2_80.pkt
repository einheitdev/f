name: "ct default allow creates entry 1.1.1.1 1024 2.2.2.2 80"
source_fw: |
  @xdp(eth0)
  allow if conntrack(pkt).state == established
  drop if pkt.proto == tcp and pkt.tcp.rst
  default allow
sequence:
  - name: "syn matches no rule and is admitted by `default allow`"
    builder: tcp(src_ip="1.1.1.1", dst_ip="2.2.2.2", src_port=1024, dst_port=80, syn=true)
    expected: { bpf_action: allow }
  - name: "rst on the same 5-tuple: established, so the first rule allows it before the rst rule can drop it"
    builder: tcp(src_ip="1.1.1.1", dst_ip="2.2.2.2", src_port=1024, dst_port=80, rst=true)
    expected: { bpf_action: allow }
