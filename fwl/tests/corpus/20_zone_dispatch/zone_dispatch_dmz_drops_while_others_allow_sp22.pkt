name: "zone dispatch dmz drops while others allow sp22"
source_fw: |
  zone wan = [wan0]
  zone lan = [lan0]
  zone dmz = [dmz0]
  zone guest = [guest0]
  @xdp(wan)
  allow
  @xdp(lan)
  allow
  @xdp(dmz)
  drop
  @xdp(guest)
  allow
ingress_zone: dmz
test_packet:
  builder: tcp(src_ip="10.1.0.5", dst_ip="1.1.1.1", dst_port=22)
expected:
  compiles: true
  bpf_action: drop
