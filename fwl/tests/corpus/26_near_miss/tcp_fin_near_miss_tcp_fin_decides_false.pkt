name: "tcp fin: near miss, tcp_fin decides false"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.tcp.fin
  default allow
# Reaches the rule and fails ONLY on `tcp_fin`. A TCP segment with FIN clear (and ACK set, so it is a segment a wire actually carries). v4_ok, proto and l4_ok all decide true and the rule falls to `fin`.
# That arm had never been taken by any corpus case: the corpus sends packets that match this rule, and packets that fail at the first conjunct and never evaluate this one.
test_packet:
  builder: tcp(ack=true, dst_port=80)
expected:
  compiles: true
  bpf_action: allow
