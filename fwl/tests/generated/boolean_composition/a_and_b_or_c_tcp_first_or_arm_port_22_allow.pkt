name: "a and (b or c): tcp + first or-arm port 22 -> allow"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == tcp and (pkt.dst_port == 22 or pkt.dst_port == 80)
  default drop

test_packet:
  builder: tcp(dst_port=22)

expected:
  compiles: true
  bpf_action: allow
