name: "and binds tighter than or - first or branch matches via src_ip"
source_fw: |
  @xdp(eth0)
  drop if pkt.src_ip == 1.2.3.4 or pkt.src_ip == 5.6.7.8 and pkt.proto == tcp

test_packet:
  builder: udp(src_ip="1.2.3.4", dst_port=53)

expected:
  compiles: true
  bpf_action: drop
