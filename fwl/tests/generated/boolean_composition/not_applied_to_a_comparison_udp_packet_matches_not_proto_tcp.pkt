name: "not applied to a comparison - udp packet matches not(proto==tcp)"
source_fw: |
  @xdp(eth0)
  drop if not pkt.proto == tcp

test_packet:
  builder: udp(dst_port=53)

expected:
  compiles: true
  bpf_action: drop
