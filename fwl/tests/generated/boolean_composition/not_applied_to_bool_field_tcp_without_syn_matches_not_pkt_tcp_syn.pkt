name: "not applied to bool field - tcp without syn matches not(pkt.tcp.syn)"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and not pkt.tcp.syn

test_packet:
  builder: tcp(src_ip="9.9.9.9", dst_port=80, syn=false, ack=true)

expected:
  compiles: true
  bpf_action: drop
