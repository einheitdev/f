name: "not on a bool field: 'not pkt.tcp.syn' does not match a syn packet"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and not pkt.tcp.syn

test_packet:
  builder: tcp(syn=true, ack=false, dst_port=80)

expected:
  compiles: true
  bpf_action: allow
