name: "not on a bool field: 'not pkt.tcp.syn' matches a tcp packet without syn"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and not pkt.tcp.syn

test_packet:
  builder: tcp(syn=false, ack=true, dst_port=80)

expected:
  compiles: true
  bpf_action: drop
