name: "not on a comparison: 'not pkt.proto == tcp' does not match tcp packet"
source_fw: |
  @xdp(eth0)
  drop if not pkt.proto == tcp

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: allow
