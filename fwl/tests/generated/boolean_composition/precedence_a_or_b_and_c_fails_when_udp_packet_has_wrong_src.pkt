name: "precedence: 'a or b and c' fails when udp packet has wrong src"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == tcp or pkt.proto == udp and pkt.src_ip == 9.9.9.9
  default drop

test_packet:
  builder: udp(src_ip="1.2.3.4", dst_port=53)

expected:
  compiles: true
  bpf_action: drop
