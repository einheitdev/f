name: "short-circuit proto guard - udp with dst_port 22 does not match tcp rule"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port == 22

test_packet:
  builder: udp(src_port=12345, dst_port=22)

expected:
  compiles: true
  bpf_action: allow
