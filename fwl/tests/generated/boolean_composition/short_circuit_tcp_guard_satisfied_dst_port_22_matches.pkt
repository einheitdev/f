name: "short-circuit: tcp guard satisfied, dst_port==22 matches"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port == 22

test_packet:
  builder: tcp(dst_port=22)

expected:
  compiles: true
  bpf_action: drop
