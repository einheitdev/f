name: "port range with lo greater than hi is rejected"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port in 1000..500
  default allow

test_packet:
  builder: tcp()

expected:
  compiles: false
