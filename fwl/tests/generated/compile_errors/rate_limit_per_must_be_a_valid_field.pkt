name: "rate_limit per= must be a valid field"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp limited by rate_limit(10, per=proto)
  allow

test_packet:
  builder: tcp()

expected:
  compiles: false
