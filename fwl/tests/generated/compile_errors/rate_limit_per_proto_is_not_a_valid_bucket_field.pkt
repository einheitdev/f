name: "rate_limit per=proto is not a valid bucket field"
source_fw: |
  @xdp(eth0)
  drop limited by rate_limit(10, per=proto)
  default allow

test_packet:
  builder: tcp()

expected:
  compiles: false
