name: "rate_limit threshold of zero is rejected"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp limited by rate_limit(0, per=src_ip)
  allow

test_packet:
  builder: tcp()

expected:
  compiles: false
