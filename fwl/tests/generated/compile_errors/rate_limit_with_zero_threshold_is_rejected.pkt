name: "rate_limit with zero threshold is rejected"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port == 22
       limited by rate_limit(0, per=src_ip)
  default allow

test_packet:
  builder: tcp()

expected:
  compiles: false
