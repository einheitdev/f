name: "count fires and packet is allowed"
source_fw: |
  @xdp(eth0)
  count tcp_traffic if pkt.proto == tcp
  allow

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: allow
