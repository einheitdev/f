name: "count fires before terminal drop"
source_fw: |
  @xdp(eth0)
  count blocked_udp if pkt.proto == udp
  drop if pkt.proto == udp

test_packet:
  builder: udp(dst_port=53)

expected:
  compiles: true
  bpf_action: drop
