name: "count fires, drop condition fails, allow terminates"
source_fw: |
  @xdp(eth0)
  count seen if pkt.proto == tcp
  drop if pkt.proto == tcp and pkt.dst_port == 9999
  allow

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: allow
