name: "log and count both fire before terminal allow"
source_fw: |
  @xdp(eth0)
  log if pkt.proto == tcp and pkt.dst_port == 443
  count https_seen if pkt.proto == tcp and pkt.dst_port == 443
  allow

test_packet:
  builder: tcp(dst_port=443)

expected:
  compiles: true
  bpf_action: allow
