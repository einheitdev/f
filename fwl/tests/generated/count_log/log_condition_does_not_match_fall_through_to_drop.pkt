name: "log condition does not match, fall through to drop"
source_fw: |
  @xdp(eth0)
  log if pkt.proto == udp
  drop

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: drop
