name: "multiple count rules fire before terminal allow"
source_fw: |
  @xdp(eth0)
  count all_packets
  count tcp_packets if pkt.proto == tcp
  allow

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: allow
