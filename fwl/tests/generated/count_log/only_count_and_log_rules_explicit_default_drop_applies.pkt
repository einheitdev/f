name: "only count and log rules, explicit default drop applies"
source_fw: |
  @xdp(eth0)
  count seen
  log if pkt.proto == tcp
  default drop

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: drop
