name: "two named counters, only one matches the packet"
source_fw: |
  @xdp(eth0)
  count tcp_count if pkt.proto == tcp
  count udp_count if pkt.proto == udp
  allow

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: allow
