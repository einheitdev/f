name: "log+log+default drop — neither log matches ICMP, default drop fires"
source_fw: |
  @xdp(eth0)
  log if pkt.proto == tcp
  log if pkt.proto == udp
  default drop

test_packet:
  builder: icmp(src_ip="6.6.6.6")

expected:
  compiles: true
  bpf_action: drop
