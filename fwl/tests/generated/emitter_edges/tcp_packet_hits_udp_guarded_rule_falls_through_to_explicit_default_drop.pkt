name: "TCP packet hits UDP-guarded rule — falls through to explicit default drop"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == udp and pkt.dst_port == 53
  default drop

test_packet:
  builder: tcp(dst_port=53)

expected:
  compiles: true
  bpf_action: drop
