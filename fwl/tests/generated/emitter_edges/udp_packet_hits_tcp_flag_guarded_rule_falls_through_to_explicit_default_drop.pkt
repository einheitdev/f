name: "UDP packet hits TCP-flag-guarded rule — falls through to explicit default drop"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == tcp and pkt.tcp.syn
  default drop

test_packet:
  builder: udp(dst_port=53)

expected:
  compiles: true
  bpf_action: drop
