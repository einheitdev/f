name: "allow if dst_port >= 1024 matches at exact boundary 1024"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == tcp and pkt.dst_port >= 1024
  default drop

test_packet:
  builder: tcp(src_port=12345, dst_port=1024)

expected:
  compiles: true
  bpf_action: allow
