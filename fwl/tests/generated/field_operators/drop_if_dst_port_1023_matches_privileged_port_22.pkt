name: "drop if dst_port <= 1023 matches privileged port 22"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port <= 1023
  default allow

test_packet:
  builder: tcp(src_port=12345, dst_port=22)

expected:
  compiles: true
  bpf_action: drop
