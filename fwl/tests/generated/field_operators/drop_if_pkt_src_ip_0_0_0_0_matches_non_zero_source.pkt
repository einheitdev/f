name: "drop if pkt.src_ip != 0.0.0.0 matches non-zero source"
source_fw: |
  @xdp(eth0)
  drop if pkt.src_ip != 0.0.0.0
  default allow

test_packet:
  builder: tcp(src_ip="1.2.3.4", dst_port=80)

expected:
  compiles: true
  bpf_action: drop
