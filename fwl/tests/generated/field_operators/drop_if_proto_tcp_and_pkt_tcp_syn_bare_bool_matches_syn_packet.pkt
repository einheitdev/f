name: "drop if proto==tcp and pkt.tcp.syn (bare bool) matches SYN packet"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.tcp.syn
  default allow

test_packet:
  builder: tcp(src_port=12345, dst_port=80, syn=true, ack=false)

expected:
  compiles: true
  bpf_action: drop
