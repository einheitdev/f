name: "drop if src_ip in private CIDR list falls through for public source"
source_fw: |
  @xdp(eth0)
  drop if pkt.src_ip in [10.0.0.0/8, 192.168.0.0/16]
  default allow

test_packet:
  builder: tcp(src_ip="8.8.8.8", dst_port=80)

expected:
  compiles: true
  bpf_action: allow
