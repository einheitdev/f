name: "dst_ip == 0.0.0.0 boundary matches and drops"
source_fw: |
  @xdp(eth0)
  drop if pkt.dst_ip == 0.0.0.0
  default allow

test_packet:
  builder: tcp(dst_ip="0.0.0.0", dst_port=80)

expected:
  compiles: true
  bpf_action: drop
