name: "dst_ip == 255.255.255.255 boundary matches and drops"
source_fw: |
  @xdp(eth0)
  drop if pkt.dst_ip == 255.255.255.255
  default allow

test_packet:
  builder: tcp(dst_ip="255.255.255.255", dst_port=80)

expected:
  compiles: true
  bpf_action: drop
