name: "dst_port < 1024 matches well-known port and drops"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port < 1024
  default allow

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: drop
