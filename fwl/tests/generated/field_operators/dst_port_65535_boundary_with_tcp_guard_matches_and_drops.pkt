name: "dst_port == 65535 boundary with tcp guard matches and drops"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port == 65535
  default allow

test_packet:
  builder: tcp(dst_port=65535)

expected:
  compiles: true
  bpf_action: drop
