name: "dst_port >= 80 inclusive boundary matches and allows"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == tcp and pkt.dst_port >= 80
  default drop

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: allow
