name: "proto != udp matches tcp packet drops"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto != udp
  default allow

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: drop
