name: "src_ip != literal does not match same IP falls through"
source_fw: |
  @xdp(eth0)
  drop if pkt.src_ip != 1.2.3.4
  default allow

test_packet:
  builder: tcp(src_ip="1.2.3.4", dst_port=80)

expected:
  compiles: true
  bpf_action: allow
