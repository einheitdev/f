name: "src_port == 0 boundary with tcp guard matches and drops"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.src_port == 0
  default allow

test_packet:
  builder: tcp(src_port=0, dst_port=80)

expected:
  compiles: true
  bpf_action: drop
