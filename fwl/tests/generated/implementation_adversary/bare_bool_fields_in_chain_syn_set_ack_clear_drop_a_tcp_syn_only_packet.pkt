name: "bare bool fields in chain (syn set, ack clear) drop a TCP SYN-only packet"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.tcp.syn and not pkt.tcp.ack
  default allow

test_packet:
  builder: tcp(dst_port=22, syn=true, ack=false)

expected:
  compiles: true
  bpf_action: drop
