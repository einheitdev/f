name: "hex port literal 0x10000 (65536) exceeds u16 range and must be rejected"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port == 0x10000
  default allow

test_packet:
  builder: tcp(dst_port=22)

expected:
  compiles: false
