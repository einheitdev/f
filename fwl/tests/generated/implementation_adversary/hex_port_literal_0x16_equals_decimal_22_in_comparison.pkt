name: "hex port literal 0x16 equals decimal 22 in comparison"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port == 0x16
  default allow

test_packet:
  builder: tcp(dst_port=22)

expected:
  compiles: true
  bpf_action: drop
