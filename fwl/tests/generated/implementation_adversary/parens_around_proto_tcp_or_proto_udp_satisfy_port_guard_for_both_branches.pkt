name: "parens around (proto==tcp or proto==udp) satisfy port guard for both branches"
source_fw: |
  @xdp(eth0)
  allow if (pkt.proto == tcp or pkt.proto == udp) and pkt.dst_port == 53
  default drop

test_packet:
  builder: udp(dst_port=53)

expected:
  compiles: true
  bpf_action: allow
