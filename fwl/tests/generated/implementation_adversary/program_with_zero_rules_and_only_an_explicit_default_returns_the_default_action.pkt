name: "program with zero rules and only an explicit default returns the default action"
source_fw: |
  @xdp(eth0)
  default drop

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: drop
