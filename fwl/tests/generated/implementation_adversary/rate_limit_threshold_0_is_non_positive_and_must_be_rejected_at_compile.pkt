name: "rate_limit threshold 0 is non-positive and must be rejected at compile"
source_fw: |
  @xdp(eth0)
  drop limited by rate_limit(0, per=src_ip)
  default allow

test_packet:
  builder: tcp(src_ip="1.2.3.4")

expected:
  compiles: false
