name: "single-line comment between rules is stripped before parse"
source_fw: |
  @xdp(eth0)
  # this rule blocks SSH
  drop if pkt.proto == tcp and pkt.dst_port == 22
  default allow

test_packet:
  builder: tcp(dst_port=22)

expected:
  compiles: true
  bpf_action: drop
