name: "long mixed port list does not match port outside set"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == tcp and pkt.dst_port in [22, 80, 443, 8080, 8443]
  default drop

test_packet:
  builder: tcp(dst_port=9000)

expected:
  compiles: true
  bpf_action: drop
