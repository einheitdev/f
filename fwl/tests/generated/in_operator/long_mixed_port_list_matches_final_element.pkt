name: "long mixed port list matches final element"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == tcp and pkt.dst_port in [22, 80, 443, 8080, 8443]
  default drop

test_packet:
  builder: tcp(dst_port=8443)

expected:
  compiles: true
  bpf_action: allow
