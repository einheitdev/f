name: "port list single element matches like equality"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == tcp and pkt.dst_port in [22]
  default drop

test_packet:
  builder: tcp(src_ip="10.0.0.5", dst_port=22)

expected:
  compiles: true
  bpf_action: allow
