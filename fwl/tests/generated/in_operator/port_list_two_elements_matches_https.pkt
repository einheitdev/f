name: "port list two elements matches https"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == tcp and pkt.dst_port in [80, 443]
  default drop

test_packet:
  builder: tcp(src_ip="1.2.3.4", dst_port=443)

expected:
  compiles: true
  bpf_action: allow
