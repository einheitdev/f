name: "port list with single element does not match other port"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == tcp and pkt.dst_port in [22]
  default drop

test_packet:
  builder: tcp(dst_port=23)

expected:
  compiles: true
  bpf_action: drop
