name: "port range 100..200 matches at lower boundary 100"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == tcp and pkt.dst_port in 100..200
  default drop

test_packet:
  builder: tcp(dst_port=100)

expected:
  compiles: true
  bpf_action: allow
