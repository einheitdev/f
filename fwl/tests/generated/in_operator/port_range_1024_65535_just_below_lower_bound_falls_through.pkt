name: "port range 1024..65535 just below lower bound falls through"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port in 1024..65535
  default allow

test_packet:
  builder: tcp(src_ip="1.2.3.4", dst_port=1023)

expected:
  compiles: true
  bpf_action: allow
