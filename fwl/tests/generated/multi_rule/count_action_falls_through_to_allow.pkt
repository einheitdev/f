name: "count action falls through to allow"
source_fw: |
  @xdp(eth0)
  count http_traffic if pkt.proto == tcp and pkt.dst_port == 80
  allow if pkt.proto == tcp and pkt.dst_port == 80
  default drop

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: allow
