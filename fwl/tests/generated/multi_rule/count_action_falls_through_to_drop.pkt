name: "count action falls through to drop"
source_fw: |
  @xdp(eth0)
  count blocked_telnet if pkt.proto == tcp and pkt.dst_port == 23
  drop if pkt.proto == tcp and pkt.dst_port == 23
  default allow

test_packet:
  builder: tcp(dst_port=23)

expected:
  compiles: true
  bpf_action: drop
