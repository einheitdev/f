name: "first-match: earlier allow shadows later drop for same packet"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == tcp and pkt.dst_port == 80
  drop if pkt.proto == tcp and pkt.dst_port == 80

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: allow
