name: "first-match: earlier drop shadows later allow for same packet"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port == 22
  allow if pkt.proto == tcp and pkt.dst_port == 22

test_packet:
  builder: tcp(dst_port=22)

expected:
  compiles: true
  bpf_action: drop
