name: "log falls through to subsequent allow rule"
source_fw: |
  @xdp(eth0)
  log if pkt.proto == tcp and pkt.dst_port == 22
  allow if pkt.proto == tcp and pkt.dst_port == 22
  default drop

test_packet:
  builder: tcp(dst_port=22)

expected:
  compiles: true
  bpf_action: allow
