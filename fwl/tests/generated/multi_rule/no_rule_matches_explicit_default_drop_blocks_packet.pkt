name: "no rule matches; explicit default drop blocks packet"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == tcp and pkt.dst_port in [80, 443]
  default drop

test_packet:
  builder: tcp(src_ip="6.6.6.6", dst_port=22)

expected:
  compiles: true
  bpf_action: drop
