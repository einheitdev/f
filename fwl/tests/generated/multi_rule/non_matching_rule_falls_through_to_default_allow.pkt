name: "non-matching rule falls through to default allow"
source_fw: |
  @xdp(eth0)
  drop if pkt.src_ip in 10.0.0.0/8
  default allow

test_packet:
  builder: tcp(src_ip="8.8.8.8", dst_port=443)

expected:
  compiles: true
  bpf_action: allow
