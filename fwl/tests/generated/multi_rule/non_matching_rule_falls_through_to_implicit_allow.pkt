name: "non-matching rule falls through to implicit allow"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port == 22

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: allow
