name: "ordering: drop ssh before allow tcp blocks ssh"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port == 22
  allow if pkt.proto == tcp
  default drop

test_packet:
  builder: tcp(dst_port=22)

expected:
  compiles: true
  bpf_action: drop
