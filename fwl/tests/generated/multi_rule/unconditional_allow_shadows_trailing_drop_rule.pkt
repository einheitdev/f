name: "unconditional allow shadows trailing drop rule"
source_fw: |
  @xdp(eth0)
  allow
  drop if pkt.proto == tcp

test_packet:
  builder: tcp(dst_port=22, syn=true)

expected:
  compiles: true
  bpf_action: allow
