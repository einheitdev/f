name: "proto guard from prior rule does not carry to next rule"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == tcp
  drop if pkt.dst_port == 80
test_packet:
  builder: tcp()
expected:
  compiles: false
