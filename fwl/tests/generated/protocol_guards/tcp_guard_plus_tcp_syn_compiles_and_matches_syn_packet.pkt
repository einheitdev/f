name: "tcp guard plus tcp.syn compiles and matches syn packet"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.tcp.syn
  default allow

test_packet:
  builder: tcp(src_port=12345, dst_port=80, syn=true)

expected:
  compiles: true
  bpf_action: drop
