name: "tcp guard + syn and ack matches SYN-ACK and drops"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.tcp.syn and pkt.tcp.ack
  default allow
test_packet:
  builder: tcp(syn=true, ack=true)
expected:
  compiles: true
  bpf_action: drop
