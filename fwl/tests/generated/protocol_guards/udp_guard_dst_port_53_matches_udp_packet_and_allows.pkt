name: "udp guard + dst_port==53 matches udp packet and allows"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == udp and pkt.dst_port == 53
  default drop
test_packet:
  builder: udp(dst_port=53)
expected:
  compiles: true
  bpf_action: allow
