name: "drop with rate_limit and no if clause fires for empty bucket"
source_fw: |
  @xdp(eth0)
  drop limited by rate_limit(100, per=src_ip)
  default allow

test_packet:
  builder: tcp(src_ip="8.8.8.8", dst_port=80)

expected:
  compiles: true
  bpf_action: drop
