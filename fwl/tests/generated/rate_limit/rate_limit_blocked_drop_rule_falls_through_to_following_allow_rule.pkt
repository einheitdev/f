name: "rate_limit blocked drop rule falls through to following allow rule"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port == 22
       and pkt.tcp.syn and not pkt.tcp.ack
       limited by rate_limit(3, per=src_ip)
  allow if pkt.proto == tcp and pkt.dst_port == 22
  default drop

state:
  rate_limit:
    0:
      "5.5.5.5": 3

test_packet:
  builder: tcp(src_ip="5.5.5.5", dst_port=22, syn=true)

expected:
  compiles: true
  bpf_action: allow
