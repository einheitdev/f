name: "rate_limit threshold=1 fires drop for first packet (empty bucket)"
source_fw: |
  @xdp(eth0)
  drop limited by rate_limit(1, per=src_ip)
  default allow

test_packet:
  builder: tcp(src_ip="3.3.3.3", dst_port=80)

expected:
  compiles: true
  bpf_action: drop
