name: "CIDR /32 acts as exact match — spec does not explicitly say /32 is the same as =="
source_fw: |
  @xdp(eth0)
  drop if pkt.src_ip in 1.2.3.4/32
  default allow

test_packet:
  builder: tcp(src_ip="1.2.3.5")

expected:
  compiles: true
  bpf_action: allow
