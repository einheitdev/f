name: "default after count — non-terminal count followed by default; ambiguous whether count 'matches' for default purposes"
source_fw: |
  @xdp(eth0)
  count seen if pkt.proto == tcp
  default drop

test_packet:
  builder: tcp(dst_port=22)

expected:
  compiles: true
  bpf_action: drop
