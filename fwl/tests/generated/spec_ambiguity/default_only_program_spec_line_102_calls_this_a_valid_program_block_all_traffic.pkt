name: "default-only program — spec line 102 calls this 'a valid program (block all traffic)'"
source_fw: |
  @xdp(eth0)
  default drop

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: drop
