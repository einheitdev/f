name: "hook-only program with zero rules — spec line 70 implicit allow when no rule matches"
source_fw: |
  @xdp(eth0)

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: true
  bpf_action: allow
