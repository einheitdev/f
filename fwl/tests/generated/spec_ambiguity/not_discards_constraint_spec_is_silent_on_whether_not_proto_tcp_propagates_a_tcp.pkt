name: "NOT discards constraint — spec is silent on whether 'not (proto != tcp)' propagates a tcp guard to siblings"
source_fw: |
  @xdp(eth0)
  allow if not (pkt.proto != tcp) and pkt.dst_port == 80

test_packet:
  builder: tcp(dst_port=80)

expected:
  compiles: false
