name: "not in via not(in) — spec defines in but never says how 'not in' or 'not (x in y)' should work"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and not pkt.dst_port in [80, 443]
  default allow

test_packet:
  builder: tcp(dst_port=22)

expected:
  compiles: true
  bpf_action: drop
