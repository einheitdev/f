name: "not pkt.tcp.syn without proto guard — spec example at 175-176 says error; not does not introduce a guard"
source_fw: |
  @xdp(eth0)
  drop if not pkt.tcp.syn

test_packet:
  builder: tcp(syn=false)

expected:
  compiles: false
