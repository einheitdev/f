name: "port literal 0 — spec says 0..65535 valid; lower boundary semantics"
source_fw: |
  @xdp(eth0)
  drop if pkt.proto == tcp and pkt.dst_port == 0
  default allow

test_packet:
  builder: tcp(dst_port=0)

expected:
  compiles: true
  bpf_action: drop
