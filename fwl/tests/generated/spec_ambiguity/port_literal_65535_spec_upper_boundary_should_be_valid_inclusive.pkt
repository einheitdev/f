name: "port literal 65535 — spec upper boundary; should be valid (inclusive)"
source_fw: |
  @xdp(eth0)
  allow if pkt.proto == udp and pkt.dst_port == 65535
  default drop

test_packet:
  builder: udp(dst_port=65535)

expected:
  compiles: true
  bpf_action: allow
